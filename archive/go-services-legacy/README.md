# Archivierte Go-Services

Dieses Verzeichnis enthält die früheren Go-Services, die als eigenständige Module unter `go-services/` geführt wurden.

## Status
- Die Entry-Points wurden auf `go/cmd` konsolidiert.
- Wiederverwendbare Logik wurde nach `go/internal/` verschoben.
- Dieses Archiv bleibt als Referenz für den bisherigen Stand erhalten.

## Hinweis
Bitte keine neuen Änderungen mehr in diesem Archiv vornehmen. Neue Implementierungen gehören nach `go/internal/` und werden über `go/cmd/*` gestartet.
