{
  "run": [
    {
      "method": "shell.run",
      "params": {
        "message": "conda install python=3.11.9 git -y"
      }
    },
    {
      "method": "shell.run",
      "params": {
        "message": "python -m venv venv"
      }
    },
    {
      "method": "shell.run",
      "params": {
        "venv": "venv",
        "message": "python -m pip install -U pip setuptools wheel"
      }
    },
    {
      "method": "shell.run",
      "params": {
        "venv": "venv",
        "message": "pip install numpy cython"
      }
    },
    {
      "method": "shell.run",
      "params": {
        "venv": "venv",
        "message": "pip install -e \".[tts]\""
      }
    },
    {
      "method": "input",
      "params": {
        "title": "GPU Backend",
        "description": "Wähle deinen GPU-Typ:",
        "type": "select",
        "options": [
          {
            "title": "🔵 CPU (alle Computer)",
            "value": "cpu"
          },
          {
            "title": "🟢 NVIDIA CUDA (RTX/GTX)",
            "value": "cuda"
          },
          {
            "title": "🟠 AMD ROCm (Linux)",
            "value": "rocm"
          },
          {
            "title": "🍎 Apple Metal (macOS)",
            "value": "metal"
          }
        ]
      }
    },
    {
      "method": "local.set",
      "params": {
        "gpu": "{{input}}"
      }
    },
    {
      "method": "shell.run",
      "params": {
        "path": "frontend",
        "message": "npm install"
      }
    },
    {
      "method": "shell.run",
      "params": {
        "venv": "venv",
        "message": "pip install -U --no-cache-dir llama-cpp-python"
      }
    },
    {
      "method": "notify",
      "params": {
        "html": "Installation abgeschlossen!<br><br><b>Python:</b> 3.11.9<br><b>GPU:</b> {{input}}<br><br>Klicke 'Start' zum Starten."
      }
    }
  ]
}