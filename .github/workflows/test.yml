name: Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  backend-tests:
    name: Backend Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.11']
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Cache pip
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ matrix.python-version }}-${{ hashFiles('pyproject.toml') }}
    
    - name: Install dependencies (pyproject extras)
      run: |
        python -m pip install --upgrade pip wheel setuptools
        pip install -e ".[dev,ci]"
=======
        pip install fastapi uvicorn pydantic python-dotenv python-multipart websockets psutil requests
    
    - name: Install test framework
      run: pip install pytest pytest-cov http
    
    - name: Run tests
      working-directory: ./backend
      run: |
        if [ -d "tests" ] && [ "$(ls -A tests/ 2>/dev/null)" ]; then
          pytest tests/ -v --tb=short
        else
          echo "✅ No test files - skipping"
        fi
  
  go-tests:
    name: Go Services Tests
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.21'
        cache-dependency-path: 'go-services/auth/go.sum'
    
    - name: Test auth service
      working-directory: ./go-services/auth
      run: |
        go mod tidy
        go test -v ./...
  
  code-quality:
    name: Code Quality
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install linters
      run: pip install -e ".[dev,ci]"
=======
      run: pip install black ruff bandit safety

    
    - name: Check formatting
      working-directory: ./backend
      run: black --check .
    
    - name: Check linting
      working-directory: ./backend
      run: ruff check .

    - name: Security scan (Bandit)
      working-directory: ./backend
      run: bandit -r . -lll

    - name: Security scan (Safety)
      working-directory: ./backend
      run: safety check --full-report --severity critical
  
  summary:
    name: Test Summary
    runs-on: ubuntu-latest
    needs: [backend-tests, go-tests, code-quality]
    if: always()
    
    steps:
    - run: |
        echo ""
        echo "================================================"
        echo "        JARVIS Core - Test Results"
        echo "================================================"
        echo ""
        echo "Backend Tests:  ${{ needs.backend-tests.result }}"
        echo "Go Tests:       ${{ needs.go-tests.result }}"
        echo "Code Quality:   ${{ needs.code-quality.result }}"
        echo ""
        echo "✅ Pipeline Complete!"
        echo ""
